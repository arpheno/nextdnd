export class TransportEncounter {
  constructor(public members: string, public map: string, public id: number, public  players: string) {
  }
}

export class Encounter {
  constructor(public members: [], public map: string, public id: number, public  players: []) {
  }
}
